const express = require('express');
const mongoose = require('mongoose');
const Package = require('../DB/Packages');
const route = express.Router();
let email_ = require('../server');

route.get('/static/packages', (req, res) => {
    res.ren;
});

module.exports = route;